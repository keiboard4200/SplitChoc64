description: Drop an input event and stop further processing

compatible: "zmk,input-processor-discard"

include: base.yaml

properties:
  "#input-processor-cells":
    type: int
    const: 0

  track-remainders:
    type: boolean
    description: Whether this processor requires fractional remainder tracking
