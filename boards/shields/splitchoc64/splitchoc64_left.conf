CONFIG_ZMK_POINTING=y

# RC23 Bluetooth:
# ZMK_KEYBOARD_NAME is "SplitChoc64" in Kconfig.defconfig and is used as
# the host-visible keyboard name during BLE pairing.
# ZMK provides five host BLE profiles by default.
